name: Challenger Azure HA Plan

on:
  workflow_dispatch:
  push:
    paths:
      - 'customers/Challenger/azure/ha/**'
      - '.github/workflows/challenger-azure-ha-plan.yml'

jobs:
  terraform:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: customers/Challenger/azure/ha

    env:
      TF_VAR_client_secret: ${{ secrets.AZURE_CLIENT_SECRET }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.5

      - name: Show Terraform version
        run: terraform version

      - name: Terraform Init
        run: |
          terraform init \
            -backend-config="resource_group_name=rg-tfstate-lab" \
            -backend-config="storage_account_name=karanktfstate001" \
            -backend-config="container_name=tfstate" \
            -backend-config="key=challenger-azure-ha.tfstate"

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: |
          terraform plan -no-color -out=tfplan -detailed-exitcode
          code=$?
          if [ $code -eq 1 ]; then
            exit 1
          fi
          exit 0

      - name: Show Terraform Plan
        run: terraform show -no-color tfplan
